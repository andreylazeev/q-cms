/**
 * Centralized lucide-react icon imports for the PageBuilder.
 *
 * Keeping every icon in one file makes it easy to audit what's used
 * (and to swap icon families later). Component files import named
 * symbols from here rather than reaching into `lucide-react` directly.
 */

export {
  AlertCircle,
  AlignCenter,
  AlignLeft,
  AlignRight,
  ArrowDown,
  ArrowLeft,
  ArrowUp,
  Check,
  CheckCircle2,
  ChevronDown,
  ChevronRight,
  ChevronUp,
  Copy,
  Download,
  ExternalLink,
  Eye,
  EyeOff,
  FileText,
  GripVertical,
  Image as ImageIcon,
  Info,
  LayoutTemplate,
  LayoutGrid,
  List,
  Loader2,
  type LucideIcon,
  Monitor,
  MoreHorizontal,
  Pencil,
  RotateCcw,
  Save,
  Search,
  Settings,
  Smartphone,
  Sparkles,
  Square,
  Tablet,
  Trash2,
  X,
} from 'lucide-react';
